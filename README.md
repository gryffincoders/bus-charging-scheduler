# Bus Charging Scheduler - Production Deployment Guide

[cite_start]This repository contains a scaling-focused Discrete Event Simulation (DES) engine built using Python and Streamlit to solve multi-operator fleet route contention[cite: 2, 5].

## Local Installation & Setup

1. **Ensure Python is Installed:**
   Make sure you are running Python 3.8 or higher.

2. **Initialize a Virtual Environment:**
   ```bash
   python -m venv venv
   source venv/bin/activate